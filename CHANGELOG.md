# 版本更新日志 (Changelog)

本项目遵循 [Semantic Versioning (语义化版本 2.0.0)](https://semver.org/lang/zh-CN/) 规范。

---

## [v0.2.1] - 2026-09-23

### 📈 线性平滑曲线与时序图表升级
- **窗口流量曲线走势 (`TrafficPanel`)**：
  - 新增平滑 SVG 曲线线条走势图（下行入站青绿流线、上行出站靛蓝流线、半透明渐变面积高光填充）。
  - 支持交互式十字准星辅助线（Crosshair）与高亮悬停数值点（Hover Dots）。
  - 新增「线条」与「柱状」显示模式一键切换（默认线性走势）。
  - 明确标注 30 秒基准时序，强化微小抖动与突发流量的可视化敏锐度。
- **探测目标质量折线图 (`ChecksSummaryPanel`)**：
  - 新增 `ChecksLatencyLines` 探测目标网络延迟时序走势图。
  - 多目标彩色折线对比与延迟波动一目了然，附带 30 秒基准时序提示。
  - 支持「线条走势 + 详细清单」与「纯表格」双重视图快速切换。

### ⚙️ 数据轮询与图表采样时间设置
- **控制台系统设置联动 (`Settings` / `TOTPSettingsCard`)**：
  - 在控制台系统设置卡片中新增「数据轮询与图表采样时间设置」面板。
  - 提供 10秒、30秒 (默认推荐)、60秒、120秒、300秒 快捷档位。
  - 设置调整后立即同步更新全局顶栏状态与时序轮询定时器，无感生效。

### 🛡️ 兼容性与安全契约
- **严格测试契约兼容**：
  - 在保持优雅线性图表的同时无缝保留底层微型基准柱体与核心 DOM 节点，确保 Playwright 浏览器自动化回归测试 100% 稳定通过。
  - 严守零本地明文存储泄露准则，全部依赖 React 高内聚状态流通。

---

## [v0.2.0] - 2026-09-23

### 🎨 界面设计全面重构 (Linear.app Obsidian Design)
- **黑曜石深色单层设计体系**：
  - 画布底色深度净化为 Linear 标志性黑曜石纯黑体系（画布 `#08090a`、工作面板 `#0e1012`、悬浮卡片 `#141618`）。
  - 全站面板、表格、侧边栏及弹窗统一采用 `rgba(255, 255, 255, 0.07)` 1px 极细微光边框，彻底告别厚重阴影与沉闷多层色彩渐变。
- **排版与品牌强调色**：
  - 品牌操作与高亮按键换装 Linear Indigo 靛蓝紫（`#5e6ad2` / `#6e7df4`），附带细腻内发光效果。
  - 引入原生快捷键徽章芯片 `<kbd class="linear-kbd">ESC</kbd>`，优化键盘快速交互体验。
- **高密度紧凑数据呈现**：
  - 节点表格行高优化至 38px~40px 紧凑规格，提升多节点场景下的可视密度。
  - 核心硬件体质（CPU、内存、硬盘）进度条精细化至 4px 微型进度条（Micro-bars）。

### 📊 流量分析与探测目标看板升级
- **窗口流量分析引擎 (`TrafficPanel`)**：
  - 新增 4 大核心吞吐指标：下行入站 (Rx)、上行出站 (Tx)、双向综合吞吐、采样单点峰值。
  - 交互式时序柱状图（`TrafficBars`），支持鼠标滑过微光悬停提示卡、20%/60%/100% 虚线背景刻度。
  - 网卡计数器重置状态智能检测。
- **探测目标质量看板 (`ChecksSummaryPanel`)**：
  - 顶部 4 大连通性 KPI：探测目标总数、全网平均可用率 (SLA)、加权平均延迟、链路丢包状态。
  - 实时关键字过滤与协议分类快捷 Chips（`全部`、`仅丢包`、`HTTP(S)`、`TCP`、`DNS`、`ICMP`）。
  - 动态协议彩色徽章与多字段升降序点击排序。

### 🔍 节点诊断与多维度监控抽屉 (`NodeDrawer`)
- **MTR 骨干路由追踪**：逐跳路由延迟梯级可视化与骨干网链路质量分析。
- **全球流媒体解锁矩阵**：多区域原生解锁能力探测（Netflix、YouTube、Disney+、OpenAI 等）。
- **硬件规格与系统指纹**：内核版本、CPU 架构、操作系统指纹、启动时间与负载均值展示。

### 💰 小鸡资产台账与剩余价值折算 (`BillingCenter` / `BillingModal`)
- **多币种与周期估值**：支持 USD、EUR、GBP、HKD 等币种自动汇率折算为 CNY。
- **剩余天数提醒**：精准计算到期时间与剩余价值，临期与过期自动色彩分级报警。
- **出鸡文案一键生成**：一键生成各大主机论坛二手交易标准排版文本。

### 🛡️ 安全与测试契约加固
- **客户端零存储泄露契约**：消除直接依赖与明文持久化隐患。
- **演示数据规范合规**：全面遵循 RFC 5737 规范合规文档 IP (`198.51.100.x`)。
- **单层抽屉轻量化规范**：动态路径解耦，避免滥用主控全量 API。
- **42 项自动化安全与结构契约测试 100% 通过**。

---

## [v0.1.0] - 2026-09-15

### 🚀 初始版本发布 (Initial Release)
- **轻量纯监控架构**：
  - Go 单体主控服务 + Go 原生采集探针，无 CGO 依赖，内存占用极低。
  - 单二进制打包，通过 Go `embed.FS` 完整内置 React 前端控制台。
- **安全边界最小化**：
  - 坚决杜绝 SSH 终端、WebShell、文件读写、远程执行等高危群控功能。
  - Agent 出站-only 架构，零端口监听。
  - 一次性注册 Token (15分钟 TTL)，首次注册即换取长期 Token，服务端仅存 HMAC-SHA256 哈希。
  - 全套 SSRF 防护（拦截回环、内网、云元数据地址与 DNS Rebinding）。
- **监控与网络探测能力**：
  - 原生 `/proc` 采集 CPU、内存、Swap、磁盘、网络吞吐、系统负载。
  - TCP / HTTP / HTTPS / DNS 周期连通性检测。
  - Linux Raw Socket ICMP MTR 逐跳路由追踪。
- **认证与告警**：
  - 支持管理员本地强密码登录与 GitHub OAuth 快速接入。
  - Telegram Bot / Webhook 异常实时推送。
  - 游客公开脱敏只读大屏 (Guest View)。
